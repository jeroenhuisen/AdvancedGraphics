#include "camera.h"
#include "template.h"


Camera::Camera(glm::vec3 position, glm::vec3 viewingDirection, float screenDistance) :
	position(position), viewingDirection(viewingDirection), screenDistance(screenDistance) {

}